#include "main.h"

/**
 * print_alphabet - Print the alphabet, in lowercase, followed by new line.
 */

void print_alphabet(void)
{
char c;

	for (c = 'a'; c <= 'z'; i++)
	{
		_putchar(c);
	}

	_putchar('\n');
}
