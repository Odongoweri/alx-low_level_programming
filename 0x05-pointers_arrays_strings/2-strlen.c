#include "main.h"
/**
 * _strlen - Count the length of a string.
 * @s: String.
 * Return: Length.
 */

size_t _strlen(const char *s)
{
	size_t length = 0;

	while (*str++)
		length++;

	return (length);
}
